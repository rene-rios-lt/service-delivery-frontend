<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!-- FE-015: App Sandbox is enabled above, so CoreLocation (MauiGeolocationService, used by the
             Requester submit form's "Use my current location") requires this location entitlement IN
             ADDITION to NSLocationWhenInUseUsageDescription in Info.plist. Without it the sandbox blocks
             location regardless of the usage string. (The Mobile host is not sandboxed, so it needs only
             the usage description — this entitlement is the maccatalyst-specific half of the same fix.) -->
        <key>com.apple.security.personal-information.location</key>
        <true/>
    </dict>
</plist>

