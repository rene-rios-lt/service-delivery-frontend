@page "/requester"

@* Requester persona home (FE-001 landing route). The Requester's home is now the submit screen
   (FE-015), so this route redirects straight to /requester/submit. Kept as a thin redirect so the
   role-routing target (/requester) stays stable while the actual home screen lives at its own route.
   Code-behind: RequesterHome.razor.cs. *@
